package;

import chrome.system.Cpu;
import chrome.system.Memory;
import chrome.system.Network;
import chrome.system.Display;
import chrome.system.Storage;
import chrome.ExtensionsManifestTypes;
import chrome.AccessibilityFeatures;
import chrome.DeclarativeNetRequest;
import chrome.DeclarativeContent;
import chrome.ContentSettings;
import chrome.PrinterProvider;
import chrome.DesktopCapture;
import chrome.ExtensionTypes;
import chrome.ContentScripts;
import chrome.ManifestTypes;
import chrome.WebNavigation;
import chrome.Notifications;
import chrome.ContextMenus;
import chrome.FontSettings;
import chrome.Permissions;
import chrome.PageCapture;
import chrome.Management;
import chrome.InstanceID;
import chrome.WebRequest;
import chrome.TabCapture;
import chrome.Offscreen;
import chrome.Bookmarks;
import chrome.Downloads;
import chrome.SidePanel;
import chrome.Extension;
import chrome.TtsEngine;
import chrome.TabGroups;
import chrome.Scripting;
import chrome.Commands;
import chrome.Identity;
import chrome.TopSites;
import chrome.Sessions;
import chrome.Debugger;
import chrome.Storage;
import chrome.Privacy;
import chrome.Windows;
import chrome.Cookies;
import chrome.Runtime;
import chrome.History;
import chrome.Omnibox;
import chrome.Events;
import chrome.Alarms;
import chrome.Action;
import chrome.Search;
import chrome.Audio;
import chrome.Power;
import chrome.Types;
import chrome.Proxy;
import chrome.Tabs;
import chrome.Idle;
import chrome.Mdns;
import chrome.I18n;
import chrome.Dom;
import chrome.Dns;
import chrome.Gcm;
import chrome.Tts;

class Main {
	static function main() {
	}
}
